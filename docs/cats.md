# گربه‌های سایت

سه گربه (مشکی، نارنجی، سفید) کل صفحه را قلمرو خودشان می‌دانند. هر گربه ۱۲ انیمیشن دست‌کشیده دارد (هر کدام ۴ فریم). کد تصمیم می‌گیرد کدام انیمیشن و کدام فریم نمایش داده شود.

## کارهایی که می‌کنند

| رفتار | کِی |
|---|---|
| دنبال کردن نقطه لیزر | موس حرکت کند (روی موبایل: تپ). گربه مشکی عاشقش است: می‌دود، کمین می‌کند، باسن تکان می‌دهد و می‌پرد |
| نشستن روی لبه بخش‌ها | با اسکرول همراه قفسه جابه‌جا می‌شوند و وقتی قفسه از صفحه بیرون رفت، می‌پرند پایین |
| هل دادن مهارت‌ها از روی میز | گاهی (بیشتر گربه سفید) روی یک مهارت می‌نشیند، به شما زل می‌زند و با پنجه می‌اندازدش. بعد برمی‌گردد سر جایش |
| رفتن توی جعبه | کارت تماس یک جعبه مقوایی است. گربه نارنجی عاشقش است و از آن سرک می‌کشد |
| خوابیدن در آفتاب | یک لکه آفتاب آرام کف صفحه حرکت می‌کند. گربه نارنجی دنبالش می‌رود و می‌خوابد |
| راه رفتن روی متن | گربه روی متن تایپ‌شده هیرو راه می‌رود و «fjjjkf» تایپ می‌کند. بعد پاک می‌شود |
| نوازش | موس را روی گربه تکان بدهید: خرخر و قلب. زیاد ادامه بدهید، یک پنجه می‌خورید |
| کلیک روی گربه | ذوق‌زده می‌پرد و ستاره پخش می‌کند |
| اسکرول سریع | جا می‌خورند، پشتشان قوز می‌کند و می‌لرزند |
| زومیز | گاهی یک گربه دیوانه‌وار کف صفحه می‌دود |
| بی‌کاری ۲۰ ثانیه | یکی‌یکی گلوله می‌شوند و چرت می‌زنند (Zzz). با اولین حرکت بیدار می‌شوند و کش می‌آیند |
| لیسیدن پنجه، کش آمدن، غلت زدن، نفس کشیدن | خودبه‌خود |

دکمه پنجه 🐾 در نوار بالا گربه‌ها را خاموش/روشن می‌کند. با تنظیم «کاهش حرکت» سیستم‌عامل، گربه‌ها فقط می‌نشینند و می‌خوابند.

## انیمیشن‌ها

| انیمیشن (نام پوشه) | کجا استفاده می‌شود | نحوه پخش |
|---|---|---|
| `sitting` | نشستن، زل زدن، سرک کشیدن از جعبه | حلقه آرام؛ فریم ۳ پلک زدن است |
| `walking` | راه رفتن، روی متن راه رفتن | فریم‌ها با مسافت جلو می‌روند (پا سُر نمی‌خورد) |
| `running` | دنبال لیزر، زومیز | همین‌طور، با مسافت |
| `crouching` | کمین | حلقه سریع |
| `leaping` | پریدن بین قفسه‌ها | فریم با پیشرفت پرش |
| `pouncing` | شکار لیزر، ذوق بعد از کلیک | فریم با پیشرفت پرش |
| `licking` | تمیز کردن | رفت‌وبرگشت |
| `reaching` | پنجه زدن، انداختن اشیا | حلقه سریع |
| `rolling` | غلت زدن، خرخر | رفت‌وبرگشت آرام |
| `sleeping` | خواب | یک بار تا گلوله شدن، بعد می‌ماند و نفس می‌کشد |
| `startled` | ترسیدن از اسکرول سریع | یک بار، بعد قوز می‌ماند |
| `stretching` | کش آمدن | یک بار |

### اضافه کردن یا عوض کردن فریم‌ها
1. **مکان فریم‌ها:** `art/frames/<انیمیشن>/<black|white|orange>/frame-01.png`، `frame-02.png` و بقیه. هر تعداد فریم مجاز است و همه باید روی یک بوم هم‌اندازه و شفاف باشند. گربه رو به **راست** باشد و پاها پایین بوم.
2. **ساخت:**
   ```bash
   pip install pillow numpy
   python3 scripts/build-frames.py
   ```
   فریم‌ها به یک نوار WebP در `public/cats/<رنگ>/<انیمیشن>.webp` تبدیل می‌شوند و `src/cats/frames.json` ساخته می‌شود. اندازه همه انیمیشن‌ها بر اساس فریم اول `sitting` تنظیم می‌شود.
3. **سرعت پخش:** در `PLAYBACK` در `src/cats/brain.js`.
4. **انیمیشن جدید:** اسمش را در تابع `pickAnim` در همان فایل به یک رفتار وصل کنید.

## تغییر دادن

همه چیز در `src/cats/cats.config.js` است:
- **`weights`:** شخصیت هر گربه. عدد بزرگ‌تر یعنی آن رفتار را بیشتر انجام می‌دهد. مثلاً `knock: 0` یعنی هیچ‌وقت چیزی را نمی‌اندازد.
- **`size`:** اندازه در دسکتاپ و موبایل.
- **`sleepAfterMs`:** بعد از چند میلی‌ثانیه بی‌کاری بخوابند.
- **`speed`:** سرعت راه رفتن، یورتمه، دویدن و زومیز.

هر عنصری در صفحه که `data-cat-surface` داشته باشد، جایی است که گربه‌ها می‌توانند رویش بنشینند. مقدارها:
- `shelf` (قفسه)
- `chip` (قابل انداختن)
- `box` (جعبه)
- `keyboard` (متن تایپی)
- `pills` (دکمه‌ها)

## فایل‌ها

| فایل | کار |
|---|---|
| `src/cats/brain.js` | مغز گربه‌ها: رفتارها، حلقه انیمیشن، ورودی موس و اسکرول |
| `src/cats/FrameCat.jsx` | نمایش انیمیشن‌های دست‌کشیده |
| `src/cats/CatSprite.jsx` | نسخه SVG قدیمی، فقط اگر عکس‌ها بارگذاری نشوند |
| `scripts/build-frames.py` | ساختن نوار فریم‌ها از `art/frames` |
| `src/cats/surfaces.js` | پیدا کردن جاهایی که گربه‌ها می‌توانند بایستند |
| `src/cats/props.js` | انداختن اشیا، ذرات (Zzz، ستاره، قلب) |
| `src/index.css` | لیزر، آفتاب، ذرات و استایل گربه‌ها، زیر بخش «Cats» |
